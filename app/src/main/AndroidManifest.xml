<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetLink">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AdoptionActivity" />
        <activity
            android:name=".AddAdoptionActivity"
            android:parentActivityName=".AdoptionActivity" />
        <activity android:name=".VeterinariansActivity" />
        <activity
            android:name=".AddVeterinarianActivity"
            android:parentActivityName=".VeterinariansActivity" />
        <activity android:name=".LostPetsActivity" />
        <activity
            android:name=".AddLostPetActivity"
            android:parentActivityName=".LostPetsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".ProfileActivity" />
        <activity
            android:name=".AddMyPetActivity"
            android:parentActivityName=".ProfileActivity" />

    </application>

</manifest>